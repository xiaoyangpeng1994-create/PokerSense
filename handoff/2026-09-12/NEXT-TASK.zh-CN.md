# 下一项任务

## 唯一当前任务

策略任务7：建立第一批**simulation-only完整组合基线和对抗回归**，为未来经
审计的真实AA范围/求解数据预留同一接口。

目标：

1. 生成6/7/8人、位置、有效筹码、翻前行动线的完整concrete-combo资产；
2. 明确所有先验和动作似然是模拟、开源派生还是求解器输出；
3. 禁止把现有MIT preflopR的无前注/无抽水RFI表重标为AA straddle策略；
4. 覆盖阻断牌、跨玩家碰牌、似然缺失/低覆盖、规则指纹变化、缓存失效；
5. 用合成权威DecisionContext跑范围→多人权益→WAL数学记录，不输出行动建议；
6. 产出版本化报告，说明覆盖率、拒绝率、耗时和没有真实准确率的边界。

## 通过标准

- 资产文件/Schema/来源/规则指纹/hash全部可重现；
- 6/7/8人每个声明节点都有具体组合且权重精确和为1；
- action likelihood覆盖率低于0.80时权益门禁拒绝；
- tracker snapshot与DecisionContext不一致时拒绝；
- test_only资产不能进入shadow，shadow_reviewed不能自动成为live；
- WAL允许记录数学结果，但拒绝preferred_action、下注尺寸等可执行字段；
- 完整pytest和flake8通过；V1冻结280文件哈希不变。

## 明确不在本任务内

- 不消费300–600秒新保留录像；
- 不开启真实采集或Advice；
- 不实现保险、种蘑菇、暴击资金语义；
- 不把合成资产包装成GTO、平台真实范围或盈利证明；
- 不下载/运行TexasSolver或其他外部求解器，除非先完成来源、版本、哈希、
  许可证、进程隔离和输入输出合同审核。

完成任务7之后，才选择：接入经审计的真实求解数据，或先做翻后river独立
resolver接口。选择必须以证据和耗时测量为依据，不由模型自行宣称最优。
