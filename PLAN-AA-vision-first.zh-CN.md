# AA 视觉主线计划

2026-09-08 用户最新授权：AA 为主线，WPK 只保留回归。本文覆盖旧计划中的
“WPK 优先、AA 第二阶段”顺序，历史报告和证据不删除。策略、客户端操作及实战不在本阶段。

## 实施顺序与交付证据

1. 原片建档：读取既有 AA session_001，冻结 SHA256 和文件属性，顺序解码，
   保存准确的解码序号和解码器时间读数、失败/时间异常；采样图只用于探索。
   容器时间不能代替手机时间，正常静止画面不能证明采集卡卡死。
2. 独立测量归一化与布局：从原图量取手机边界，验证全片稳定性后冻结配置。
   九个物理槽位与实际入座人数、参与本手人数分别表达；重点覆盖 6/7/8 人。
   不复用 WPK 坐标、模板、置信门槛或八座假设。
3. 建立手级样本登记：区分参与、弃牌、等待、旁观、未入桌；保留整手及负样本。
   先确认真实手边界，再按整手冻结开发/校准/验收集合，记录所有人工和模型曝光。
   已看过的探索画面不能冒充未曝光的独立验收样本。
4. 字段适配：Hero/公共牌、底池文字前缀、筹码/下注、庄位、入座、动作和轮到谁。
   Hero 余额在手牌上方；保险、STR、动画/菜单及未知特殊模式必须单独标记。
   读不清返回 UNKNOWN，不把缺按钮当旁观，不把余额当本手可用筹码。
5. 连续视觉链：复用牌面生命周期和因果证据机制，将八座硬编码改成布局驱动，
   仅在必要处改共享代码，每次改动跑 WPK 回归。跨手、掉帧、遮挡、换桌清理旧状态。
6. 独立离线验收和界面接口：输出当前画面来源、字段置信/拒识原因、参与状态和失效状态。
   以完整连续手牌核验，不以几张截图或单元测试替代。

## 可搭策略层的门槛

- 冻结代码、配置、来源哈希与手级分割；记录每字段正确/误收/拒识数及分母。
- 6/7/8 人普通桌分别有未用于调参的完整手级验收；九槽布局不得丢失或串座。
  缺少任一必要场景就报缺口，不用合成录像补作真实证据。
- 验收集合内不得出现已知错误牌面/串座被接受，旁观和已弃牌不能成为可操作状态。
  同时报告覆盖率与拒识率，禁止靠全部拒识宣布通过。
- 普通手的必要字段和行动顺序逐项对齐审阅真值；未验证的保险/特殊模式明确拒绝，
  缺动作不能补造。每种支持模式单列是否具备策略输入条件。
- 全片连续回放完成，异常恢复、跨手清理及确定性复跑通过；报告实测耗时与内存。
- WPK 已有回归无新增失败。真实采集时延/硬件可靠性仍须后续单独验收。

完成时状态才是 `AA_VISION_READY_FOR_STRATEGY / needs_review`，通知用户审阅。
这不意味着实战可用、盈利有保证或产品 100% 完成。样本不足时指出具体所缺片段。

## 当前工作队列

### 2026-09-09 素材模式纠正（最新）

用户纠正：现有录像出现的是种蘑菇，并未出现鱿鱼。当前素材主线为暴击、种蘑菇、
保险及抽水/结算；蘑菇与鱿鱼必须使用独立标签、状态和规则，不当作同义词。
鱿鱼的未来产品支持需求保留，当前录像不能提供其验收证据，也不再反复搜寻本片
不存在的鱿鱼实例。下文较早的“本片有鱿鱼”假设已被此条覆盖。

### 最新执行授权：四项必须一起达到验收门槛

用户明确要求补完：逐座位字段/参与状态、特殊模式、连续恢复/换手、独立及长时验收。
不得仅完成其中一项就宣布可以接策略。当前实测和缺项见
`docs/AA-COMBINED-FIELDS-V1.zh-CN.md`，特殊机制证据见
`docs/AA-SPECIAL-MODES-EVIDENCE.zh-CN.md`。全片解码与已完成的局部回归不要重复建档。
保留区间不可用于调参；未通过验收不得启用实战或策略。

### 用户补充：暴击与鱿鱼游戏（必做）

- 视觉必做：识别普通桌、暴击、鱿鱼游戏及无法确认的模式；不假定两种特殊机制互斥。
  分别记录“房间配置中启用”“本手触发”“结算展示”，不能看到规则说明就当本手触发。
- 可见的标志、提示文字、参与者标记、计数/金额、开始/结束或结算动画分别建真值，
  实际出现哪些字段由原片核实，不凭名字补造罚金、倍数或触发条件。
- 覆盖模式进入/退出、动画遮挡、普通桌相似图标等负样本；缺少实例时报告素材缺口。
  不可见或冲突状态输出 UNKNOWN，不能默认为普通德州或沿用上一手模式。
- 策略阶段必须有暴击与鱿鱼游戏的独立规则适配、金额核算和策略验证任务，
  由核实后的房间规则驱动配置；本阶段只做视觉输入与规则需求登记，不实现下注建议。
- 完整 AA 验收包含这两个模式的视觉覆盖。仅普通桌通过时只能报告普通桌范围的
  PARTIAL，不能宣布全部 AA 已可接策略；特殊规则尚未核实则保留该模式策略禁用状态。

- 已完成：来源哈希、41,339 帧顺序解码、46 张探索原图和候选裁剪目录。
  证据与边界见 `docs/AA-SOURCE-AUDIT-V1.zh-CN.md`；不要重复建档。
- 待做：裁剪冻结、九槽几何、手级真值及隔离集合、识别器、时序、独立验收。
- 已推进（未验收）：九槽候选几何、AA 场景探测、九槽时序参数化；
  121 张密集开发样本、11 个先冻结真值检查点，以及牌背/上移裁剪拒识保护。
  详见 `docs/AA-GEOMETRY-CARD-BASELINE-V1.zh-CN.md`，不要重做来源建档或这批迁移基线。
- 原片不修改，派生数据放 `G:/PokerSense_archive/aa_video_first_20260908`。
- 不启动定时任务；不重复采集已有录像；不改 WPK 已有标签和生产权重。
