"""Core domain layer (frozen contracts).

Progress:
- Task 1B: enums, value objects (ChipAmount/ChipDelta/Card), errors.
- Task 1C: observation / state / opponents(PlayerState) / events /
  request_context contracts.
- Task 1D: hand (HandHistory/HandSummary) / reports / opponents(OpponentProfile).
- Task 1E: serialization (serialize/deserialize) + docs.
"""
