# Historical V6 experiment environment, NOT the application's release pins.
# Windows x64, CPython 3.13.14; install in a fresh venv without system packages.
# Exactly one OpenCV distribution is intentional. Do not also install
# opencv-python or opencv-python-headless into this environment.
numpy==2.3.5
opencv-contrib-python==4.10.0.84
scipy==1.18.1
scikit-learn==1.9.0
joblib==1.6.0
threadpoolctl==3.6.0
narwhals==2.25.0
cloudpickle==3.1.2
