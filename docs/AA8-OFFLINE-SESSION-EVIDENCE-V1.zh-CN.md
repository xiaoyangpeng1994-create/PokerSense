# AA8 物理采集卡离线会话证据 V1

本轮落实两个约束：雷电及其他模拟器不再是产品可选输入；AA 主线继续使用 G 盘已有真机采集卡录像做离线开发。没有启动模拟器、ADB、采集设备或实时策略。

## 输入隔离

新八座录像 `aa_phone_record_20260909_031030_54322c62` 已有完整性报告：17 段、29865 帧、995.366 秒。原冻结分组保持不变：0–300 秒和 820–995.366 秒为开发；300–600 秒为校准候选；600–820 秒为保留验收候选。

本轮只解码 `segment_0014.mkv`、`segment_0015.mkv` 和 `segment_0016.mkv`，范围 840.023–995.366 秒，共 4661 帧。`segment_0013.mkv` 从 780.032 延续到 839.989 秒，跨越保留区到开发区边界，整段排除，没有读取。所有源分段沿用完整性报告中的大小、修改时间、帧数和 SHA-256，提取前后不变。

归一化输出和私有清单位于 `G:/PokerSense_private/aa8_late_development_full_20260914_v1/`。原始帧、玩家显示名和审查图片不进入 Git。

## 边界复核

现有识别链对 4661 帧完整回放。公共牌和关键画面人工复核后登记两手时间完整的开发候选及一个录像尾部残段：

| ID | 拥有帧范围 | 最后牌局画面 | 下一手证据 | 结论 |
|---|---:|---:|---:|---|
| `aa8_late_dev_hand_005` | 25492–27473 | 27349 | 27474 | 时间完整；开局含幸运暴击/特殊底池，资金语义 PARTIAL |
| `aa8_late_dev_hand_006` | 27474–29051 | 28928 | 29052 | 时间完整；一空座及多座开局投入可见，完整资金账本仍 PARTIAL |
| `aa8_late_dev_tail_007` | 29052–29864 | 29052 | 无 | 录像尾部未出现完整结束，明确 INCOMPLETE |

注册表 `configs/reproduction/aa8_late_development_hands_v1.json` 只保存分段、窗口、关键帧和 SHA-256，不保存原图或玩家名。`aa8_offline_session_evidence.py` 先只解析清单文字并验证根内路径、窗口、逐帧 PTS、segment/local/global frame、角色和声明集合；全部通过后才读取并核对 4661 个已声明开发帧。未声明图片和 review sheet 不读取。它随后绑定识别日志和关键帧证据；哈希、顺序、角色或保留区有任何偏差都会拒绝。

## 决策机会现状

识别链得到 42 个动作字形候选；完整两手各 18 个，共 36 个，其中对手候选 30 个。29/36 能在确认帧及此前 12 帧中找到同座位行动者候选。类型为 14 fold、13 check、6 call、2 aggressive、1 all-in。

这些数字是复核队列，不是动作准确率。字形出现帧可能晚于真实点击时刻；一个候选甚至可能包含同一行动的短暂错误状态。每条动作的完整合法菜单、精确动作金额和独立动作真值仍为 0，因此 `ready_for_opponent_calibration=false`。同一次 16 分钟录制也不能拆成训练/验证后冒充跨会话验证。

私有输出 `G:/PokerSense_private/aa8_offline_session_evidence_20260914_v1/report-v4.json` 完整保留 36 条候选及缺失字段。它没有生成范围模型、策略资格或 Advice。

## 模拟器入口关闭

桌面应用和本地服务的用户可选采集源现在只有 `capture-card`；`--source adb` 在参数层和程序化工厂层都会拒绝。ADB 后端代码仅用于解释历史标定和离线回归，没有删除旧证据，也不能重新暴露为产品入口。

README 中英文版和 AA 客户端说明同步更新。当前主线仍是录像优先；未来实机验收只能使用实体手机采集卡，Windows 原生客户端窗口捕获可另行标定，但当前不是已支持入口。

## 下一步

先对这 36 条候选逐项核对动作前锚点、合法动作菜单、金额和真实动作，并保留错误/遮挡项；本次两手只能形成一个开发会话。还需另一个同规则完整物理会话，预先冻结整会话训练/验证划分，才可运行真实对手响应校准。

## 工程复验

28 项聚焦测试通过；全仓 3237 passed、1 skipped、2 项依赖弃用警告（28.08s）。全仓 flake8 为 0，策略夹具生成器检查通过，公开文件名检查 0 项命中，`git diff --check` 通过。最终私有报告 SHA-256 为 `1836cfe4deb3ceacc724aca2d7b2c0bd02136c1d4e88e2d75c2087152c19205f`。独立复审发现并推动修复三轮隔离问题；最终复验保护区 PTS 反例在读取 0 个 PNG 的情况下拒绝，结论 PASS。GitHub PR 检查另行记录。
