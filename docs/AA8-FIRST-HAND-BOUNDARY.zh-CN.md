# AA 八人桌第一手：开发目录与边界登记

状态：一手开发牌的可见投入边界已绑定；完整动作/资金真值尚未完成，不是独立验收通过。

## 使用范围

只处理新录像0–300秒开发区间。按冻结帧索引顺序解码，不用名义FPS猜源帧号；
保留原片，未重录、未读取校准/独立验收区间作调参。提取60个约5秒间隔的粗审样本，
再对第一手入口/出口做半秒与逐帧细化。临时样本可能重复，不能当成新增独立样本。

## 已锁定的一手

- 开发ID：`aa8_dev_56dd_001`，不是平台牌局号。
- Hero显示5d6d；第一次被人工粗审确认的正面牌为源帧1500，不声称这是最早亮牌帧。
- 本手采用“首次可见新手投入到下一手首次投入前一帧”的归属约定。
- 帧1262（42.067秒）：Hero余额200，尚未出现本轮投入。
- 帧1263（42.100秒）：多座位余额改变并出现投入，Hero变196。
- 帧3319（110.633秒）：Hero余额516，下一手尚未投入。
- 帧3320（110.666秒）：出现下一手投入，Hero变514。
- 因此本手归属1263–3319，共2057个源帧，跨segment0000和segment0001。

四张边界图与源索引/审计哈希绑定；重复生成registry.json字节相同，SHA256：
`97d8319d96506ad8681e6c7b3f13ff80cc0280f71c5e27da5081995f90cb5877`。

## 必须保留的状态区别

开头画面有“补盲或过庄”“等待下一手”文字，不因缺手牌就标成旁观。
第一手为等待入局后的首手，不假定强制投入只包含普通盲注/前注。
初始可见投入槽位为0/1/2/3/4/5/7，槽位6为空；结束前槽位6已出现等待入局者，
不能回填进本手对手名单。下注组成、完整位置分配、抽水和利润均未由此核算。

## 其他候选

- 9dQd：下一手开始3320已由前手出口确认，结束仍需细化；4351明确出现Hero弃牌文字。
- 2cTd：开始/结束仍为区间；5851明确Hero弃牌，同时顶部槽位0显示等待下一手。
- 4cQd：已找到从发牌到河牌后的过程候选，精确边界及参与状态仍待审核。
- 开头已在进行的手、等待入局过程和300秒末尾的未完成手保留，不伪装成完整验收手。

## 工具与私有证据

`sample_aa8_development`拒绝跨入校准/验收范围；`bind_aa8_hand_boundary`拒绝缺边界图、
索引缺帧、角色冒充独立验收或边界不相邻。脱敏真值在
`tests/fixtures/aa_reference_hands/eight_session_boundary_v1.json`。

私有根`G:/PokerSense_private`：`aa8_development_boundaries_v1`、
`aa8_first_hand_entry_v1`、`aa8_first_hand_exit_v1`、对应`*_dense_v2`、
`aa8_first_hand_registry_v1/v2`。全仓2702测试通过，1平台跳过、2依赖警告；lint通过。

下一步：读取这2057帧，建立逐动作、下注和结算的人工核对真值，再接八人桌自动识别。
本轮没有对新录像运行识别模型，也没有启用策略或触碰独立验收区间。
