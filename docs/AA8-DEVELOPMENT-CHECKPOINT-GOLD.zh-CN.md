# AA8 开发检查点人工真值

2026-09-09。仅稀疏开发检查点，不是全帧真值、独立holdout或发布验收。

新增 `tests/fixtures/aa_reference_hands/eight_dev_checkpoint_gold_v1.json`。
直接查看首手9张、次手3张完整PNG，人工读取行动者、Hero牌面、公共牌身份、
底池、各座余额及画面明确的参与状态。没有读取模型预测来填写标签，也没有
根据参考动作次序反推行动者。每行包含原PNG SHA256、审计SHA256、PTS、
开发角色、源池和图像文件名。12个源文件实际哈希均与清单一致。

| 帧 | 可见行动者 | 底池 | 明确行动者证据 |
| --- | ---: | ---: | --- |
| 1500 | 4 | 43 | Hero弃牌/加注/跟注18可操作按钮与计时环 |
| 1650 | 7 | 81 | 7号13秒文字和环 |
| 1950 | 2 | 99 | 2号13秒文字和环 |
| 2400 | 5 | 173 | 5号14秒文字和环 |
| 2550 | 1 | 231 | 1号13秒文字和环 |
| 2700 | 3 | 289 | 3号14秒文字和环 |
| 2850 | 1 | 289 | 1号14秒文字和环 |
| 3000 | 5 | 289 | 5号15秒文字和环 |
| 3150 | 4 | 503 | Hero弃牌/All in可操作按钮与环 |
| 4050 | 4 | 76 | Hero弃牌/加注/跟注38可操作按钮与环 |
| 4755 | 0 | 474 | 0号15秒文字和环 |
| 4890 | UNKNOWN | 578 | 无明确下注行动者，投保6不是下注轮到谁 |

首手Hero为5d6d；公共牌依次5h6c6sTc3d。次手Hero为9dQd；本批转牌画面为
7h5h2sTc。弃牌后仍可读取Hero牌面，不能因弃牌就丢掉可见事实。

3150的5号仍有背牌，未显示弃牌，标记active；不可沿用过去摘要中的“已弃牌”。
6号从空位到明确“等待下一手”，没有余额数字处是NOT_APPLICABLE，不是0。
4050的6号已背牌/跟注，属于当前手。4890的0号和3号亮牌并不独立提供当前
下注参与状态，本行参与字段PARTIAL，这两个座位保留null；其余明确弃牌可用。
4890不把缺计时器改成actor=NONE，也不把投保标签当加注。

格式：`checkpoints[]`内`fields`各字段带status/value；普通字段KNOWN，
4890 actor为UNKNOWN，参与状态PARTIAL。比较器可按座统计参与字段中非null
真值，不能把null当预测错误，也不能将UNKNOWN与UNKNOWN相等算识别成功。

完整JSON解析、12行唯一帧号、12份源PNG哈希、8座余额映射及各帧牌码无碰撞
已检查通过。没有运行识别器来修改真值，没有读取holdout或启动采集。
