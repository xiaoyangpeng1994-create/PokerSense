# 策略层任务3：庄位、整手投入候选与翻前行动线

2026-09-09。状态：开发候选闭环已完成；尚未独立验收，不能进入实战策略。

## 庄位

复用既有白色圆盘/黑色D字检测器，仅新增AA 498x1080八座固定ROI和两帧
稳定确认。换手、断帧、特殊模式或epoch变化会清理旧庄位；单帧移动动画
不更新。

开发证据：

- 第一手稳定7号庄位；第二手稳定0号；下一手继续移动到1号；
- 后段已审阅开发帧验证4号庄位移到5号；
- 1263/1266庄位移动动画各出现单帧5号/6号候选，均未通过两帧门禁；
- 15个预先人工核对的完整PNG检查点全部匹配；
- `aa8_candidate_v2_development_v6`全3839帧：稳定7号2043帧、0号1491帧、
  1号38帧、未知267帧。未知主要来自手牌epoch未建立、特殊模式及转换窗口。

输出仍为`canonical_verified=false`，策略桥接不会因开发匹配自动信任庄位。

## 整手累计投入候选

`AA8HandLedgerCandidate`从新手多座余额扣减建立8座初始向量，再将同一epoch
的动作金额逐项只应用一次。换手重置；断帧、保险/阻断或金额/座位不合法会
暂停或保持未知。未来帧动作不能提前应用。

逐帧同时记录：8座累计投入、合计、显示底池、二者差额、开局证据、已应用
动作数和污染原因。差额语义固定为“观察投入减显示底池”，不得自动归为
抽水、保险、蘑菇、暴击或利润。

开发结果（排除107个特殊模式正证据帧）：

- 整手投入候选3610/3732帧，未知122帧；
- 第一手长期观察差额6；第二手长期差额2；第三手截断窗口结束时差额-2；
- UI金额先变化、动作两帧后确认时会短暂出现负差额，仍非费用证据；
- 特殊模式开始后不跨阶段补动作，104保险交界仍安全拒识。

## 翻前行动线

复用现有Provider词表：`unopened`、`limp`、`multi_limp`、`raise`、
`three_bet`、`squeeze`、`iso_raise`、`four_bet`、`all_in`。只按同一epoch中
已观察动作的时间顺序形成候选；非法座位、未知类型、倒序/重复帧返回未知。

排除特殊模式后，所有已确认preflop的1896/1896帧都有行动线候选。开发示例：

- 第一手：`unopened → raise`；
- 第二手：`unopened → limp → multi_limp → iso_raise`；
- 翻后不套用翻前词表，明确归为“翻后Provider尚未发布”。

行动线仍为“到当前帧为止的观察候选”，完整历史和法律动作尚未独立验证，
所以`complete_and_canonical_verified=false`。

## 回归与影子后台

V8当前代码全量开发回归保持：21+17个有文字动作全部匹配，无额外、无街错；稀疏
59字段全部匹配；本轮投入2740候选/1099未知。新增9个策略状态开发检查点，
庄位/epoch/累计投入/未分配差额/行动线共45项全部匹配。

影子会话`G:/PokerSense_private/aa8_shadow_sessions/aa8-dev-v8-shadow-20260909-v1`
包含3839条哈希链记录，WAL SHA256为
`7c600afbcdda7540e86f19a25c4975e3ce6e63434223446c9e9b6d7523bbc40f`。
它把候选已存在和证据完全缺失分开统计；特殊107帧继续隔离。

## 仍未完成

- 新庄位、累计投入及行动线没有使用未参与开发的完整手牌验收；
- 长期差额6/2和第三手-2仍未唯一解释，不能满足资金守恒门禁；
- 当前手牌边界、动作、参与状态仍是candidate；
- 当前录像含特殊机制/规则痕迹，不能伪称普通AA基础桌独立样本；
- 翻后完整Provider、前注/3%抽水/straddle策略仍未实现；
- 未运行权益、Provider或Advice，未打开采集设备。

因此这轮完成的是三个P0字段的自动候选链和可追溯日志，不是策略放行。

最终全仓3141项通过、1项跳过、2条依赖弃用警告；flake8为0（沿用已有
`aa_record_session.py`排除项）。V1冻结280个文件重新计算SHA256，0个变化。
V8预测SHA256为
`ee6e6240876f62b1dd2a730b8efe6d93d5c6ce486e522c342546b7f32c1b3d20`；
V7与V8实际观察输出逐字节一致，新增异常输入防护未改变正常开发结果。
