# AA 开发 session 001：首次离线识别回放

用户授权合并 PR #12 并运行九手离线回放；PR #12 已合并为
`3b2c8b9db2a8f21949d2723a38b52e879a62dadc`。

只对既有 development-selection-v1 的九手、13,415 帧输出识别结果。
逐段顺序解码用于保持全局帧索引，被排除的帧不进入识别器或输出。
每手重新初始化 CandidateStateV2，复用已有训练模板与模型，不重新训练。
脚本以明确区分的训练 audit 和目标 session receipt 绑定来源；公共牌使用
目标源身份。动作字样跟踪复用 GlyphTransitionsV2，并显式传入遮挡暂停状态。
这是一份私有回放组合脚本，不是已发布的新生产入口。

## 结果

| 项目 | 数量 | 含义 |
|---|---:|---|
| 回放帧数 | 13,415 | 与九手登记逐帧集合一致 |
| 底池有候选值 | 13,173（98.20%） | 不是准确率 |
| 行动者有候选值 | 9,675（72.12%） | 缺失不自动视为错误 |
| 翻牌后公共牌几何帧 | 5,305 | 3/4/5 张可见候选 |
| 上述帧公共牌全部读出 | 5,126（96.63%） | 未与独立人工真值比较 |
| 稳定动作字样事件 | 100 | 不是100条已验收的真实动作 |
| Hero 洞牌输出 | 0 | 符合此前旁观样本特征 |
| 建立牌局 epoch | 0 | 状态恢复尚未闭合 |
| 完整合法状态 / Advice | 0 / 0 | 策略资格保持关闭 |

各 slot 的筹码候选覆盖次数为：0=13313、1=1553、2=11718、3=13351、
4=0、5=11742、6=3035、7=12459。分母均为13415帧，但包含空座/等待场景，
不能将缺失率直接解释为识别错误率。

优先问题是底部 slot 4 筹码全程未读出，以及所有帧 epoch 为空。
下一轮需要对底部座位的 ROI、数字字形和旁观状态初始化条件做定点复核；
不能根据手工分段直接填造运行时合法状态。人工边界登记仍是开发定义，
不等于独立完整状态验收。尚未完成逐项人工准确率对照。

## 验证与证据

输出唯一帧集合、手牌归属、策略关闭标志均通过独立元数据检查。
训练配置和模型在运行结束时与开始时哈希一致；200个实现文件的运行中快照
在结束时复核一致（不是运行前冻结证据）。

私有目录：`G:/PokerSense_private/aa-development-001-replay-v1/`。
保存 run.py、输入清单、observations.jsonl、逐手报告、audit.py及审计报告。
输出日志 SHA-256：
`5ac0941bab6e9d8845a089fa0206d1df6c851af8ca75b9b7a6186159e236c167`。
未上传原始视频、训练图片、派生帧或私有日志。未启动新采集。

## 2026-09-15 更正说明：PR #15 / #16 合并后的状态

本文件上文完整保留，记录首次九手回放当时的结果；不是当前实现的未修复清单。
本更正只依据已合并文档逐项核对，不重跑录像、不重写历史日志或历史检查结果。

| 原文陈述 | 核对结论与当前解释 |
|---|---|
| PR #12 的合并提交、九手13,415帧范围、逐手初始化、训练/目标来源区分和私有组合脚本 | 仍成立，属于首次回放的方法和范围；后续变更不回填历史结果。 |
| 底池13,173、行动者9,675、翻牌后5,305帧及完整公共牌5,126帧 | 保留为原日志统计；PR #15仅定点重算底部筹码和上下文，不能将这些旧统计称为新版全链回放结果。 |
| 各座筹码覆盖，尤其slot 4=0 | 历史成立；PR #15新增本session布局候选，将slot 4裁剪y从914改为912，候选覆盖达到13,415/13,415。7个开发抽查值匹配，但不是全帧准确率验收；旧冻结布局、模型未修改。 |
| epoch全程为0，优先排查状态初始化 | 历史成立；PR #15新增显式人工开发边界上下文后，有效epoch为12,320/13,415。其余1,095帧在两处真实保险提示后失效，保护保留。自动发牌模板仍未修复，人工上下文不代表合法状态。 |
| 100条稳定动作字样，不是100条已验收真实动作 | 仍需保持这一限制。PR #15复核84条候选，确认2条盖牌误报及H05全下漏记；PR #16开发回归保留82条已确认动作、移除2条误报、补出H05全下候选（自动确认帧8676）。新模板与回归素材重叠，不证明独立准确率或完整召回率。 |
| Hero洞牌输出0、完整合法状态/Advice为0，不能按人工分段填造合法状态 | 保留原结果及限制；PR #15/#16未开放合法状态、实时入口或策略资格。 |
| 九手开发登记及其边界 | 首次输出范围不变，但不能理解成当前有九手普通牌局。PR #15确认H02/H11含保险，且H09为幸运暴击后的牌局；保守v3普通选择为6手8,266帧，旧选择和特殊素材保留，不能用后验子集覆盖冒充全session性能。 |
| 尚未完成逐项人工准确率对照 | 首次回放时成立；PR #15后续有7个筹码抽查和84条动作候选复核，但仍没有完整独立准确率、全机会普查或资金守恒验收。 |
| 帧集合/归属/关闭标志审计、模型配置哈希、200实现文件运行中快照 | 保留为首次回放证据；“独立元数据检查”仅指单独运行的检查脚本，不是独立人员或独立holdout验收；运行中快照也不是运行前冻结证据。 |
| 私有目录、文件清单、输出日志SHA，以及未上传媒体/未启动新采集 | 保留为首次回放事实；本次冲突处理没有访问媒体或改动私有证据。 |

后续依据：[PR #15](https://github.com/xiaoyangpeng1994-create/PokerSense/pull/15)、
[PR #16](https://github.com/xiaoyangpeng1994-create/PokerSense/pull/16)，以及
[底部筹码与上下文](AA-OBSERVER-REPLAY-FIXES.zh-CN.md)、
[动作复核](AA-ACTION-REVIEW-001.zh-CN.md)、
[字样补充V3](AA-GLYPH-SUPPLEMENT-V3.zh-CN.md)。

因此本文件保留为修复前基线记录，而非被后续PR完全替代。当前剩余限制是独立
字样混淆/漏记验证、完整合法状态及自动手牌初始化，不再把底部筹码全程未知和
已确认的两条盖牌误报列为当前尚未处理的问题。
