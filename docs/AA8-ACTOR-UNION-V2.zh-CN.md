# AA8 actor V2：4/8连通候选并集与真实个位数计时器

新增`tools/aa8_actor_ring_v2.py`，不修改V1或原源模板提取逻辑。
原4755模板仍只调用四连通提取，并要求唯一模板；推理端才合并4/8连通候选。
相同归一化字形按像素哈希去重，大块头像背景仍被形状尺寸限制拒绝。
保持原0.07亮圈阈值、0.85后缀阈值、归一化尺寸与重心对齐参数。

接口：`actor_ring_v2(image, profile, original_timer)`。
可选`ContextReaderV2`继承原CandidateReader，仅替换actor观察，不改下注或余额。
没有完整秒数OCR，`timer_text_verified=false`。

## 实际开发素材

严格按现有开发采样器提取295–299.99秒，共150帧8851–9000；未触碰300秒及以后。
在8851看到2号10s；8861–8863确实出现个位数9s；8864该座弃牌，轮到3号，
因此本段没有8s。证据`aa8_timer_295_299_dev_v1`保留源审计、PTS和PNG哈希。

`aa8_actor_ring_v2_dev_v3`包含上述150帧加之前15个正例/亮牌保险负例：

- 新旧actor输出165帧全部相同，没有凭此宣称识别率提升。
- 8861–8863新旧均识别2号，后缀分数约0.947、0.942、0.936。
- 2700/2850侧座正例保留；4824亮牌与4890保险仍UNKNOWN。
- 新并集在合成测试中能够保留被四连通断开的斜向小笔画，同时不破坏白头像
  背景桥接情况下四连通已有的正确分离。

5项测试通过、flake8通过。真实个位数识别已有证据，但本批没有找到旧方法
个位数失败、新方法修复的真实案例；不因需要改进而虚构经验收益。
