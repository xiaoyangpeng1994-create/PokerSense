# AA8 开发录像特殊模式证据搜索

2026-09-09。目的是寻找真正的模式提示，不将固定房间规则文字当作触发。

## 已执行范围

- 复用 `G:/PokerSense_private/aa8_development_boundaries_v1` 的60张原有5秒采样，
  覆盖0–300秒开发区域；没有重新提取早期样本。
- 用既有受分区约束的采样器新增820–995.366秒的36张5秒间隔开发帧：
  `G:/PokerSense_private/aa8_late_dev_special_search_v1`。
- 新增 `tools/aa8_special_review_sheet.py` 生成完整画面的人工审核图，
  每张源PNG的哈希与开发角色经检查，不读取校准300–600秒或保留600–820秒。
- 已逐张审核96张采样在13张完整画面联系表中的内容，并打开以下两张原尺寸正证据。
  联系表位于 `aa8_early_special_review_v1`、`aa8_late_special_review_v1`。

## 新发现的保险几何正证据

源根目录：`G:/PokerSense_private/aa8_late_dev_special_search_v1/frames`。

| 源帧 / PTS | 人工可见事实 | PNG SHA256 |
|---|---|---|
| frame_025204.png / 840.023s | 第5座左下购买剩余30秒；双方已亮牌 | e1a28dcc07c2d5dd4f6dd32114e5ea7f41bb38605273ece474697453f6ec554c |
| frame_028654.png / 955.005s | 第7座左上购买剩余29秒；Hero余额0且全下 | 177edc5c665aa9554a884602125301d858218f7d83eff7876ab92b143d5a11b4 |

两张都是保险购买窗口的字面正证据，不是投保成功或保费扣除证据。
搜索时未修改模型；下面的后续扩展把这两帧明确转为开发模板，
因此不能再把它们算作未曝光的泛化验收。不能声称已经覆盖所有座位。

## 种蘑菇与暴击

本次96张采样中未看到可明确确认的暴击触发、种蘑菇触发面板。
这不证明整个录制没有这些事件：5秒采样可能漏掉短暂动画。

可继续细查的计数变化候选：

- 早期40秒附近显示42，45秒显示6；此前已有首手边界密集帧可以复用。
- 915.011秒帧27454显示12，920.011秒帧27604显示6。
  两帧之间包含换手，不把计数减少直接解释为发奖、扣费或触发。
- 其他初期计数依次可见36、42、6、12、18、24、30；后期6、12交替。
  静态规则仍是蘑菇3BB、暴击7BB，不能由此反推出具体支付规则。

915.011秒PNG哈希：
`fa5e79d2c7261fd30a3f7d0b168b996174796b9b17c08d9ed8f211df495e6d6f`。
920.011秒PNG哈希：
`6a9001786055bcea20ffb5805945ba1e7ebbbc01bcdfdb0481fb045cefb9cc1f`。

## 新的遮挡负样本

970.003秒帧29104到结束采样：中央大面积“带入申请”浮层，Hero余额0，
下方出现留座倒计时。不能继续当作无遮挡的在手参与者输入。
29104 PNG哈希：
`2f7511ea0ebf294f2d548411f152b758e51332bba1d353cbd0b888a4ab00cdac`。

## 结论

发现两种新的真实保险座位正例，以及新的遮挡/等待负例；
未找到足以确定种蘑菇、暴击触发与资金流转规则的正例。
这个结果用于制定后续细查，不是视觉发布或特殊模式验收通过。
原录像、全部失败、校准与保留分区均保留；未启动录制或设备。

## 后续有界空间适配

新增 `tools/aa8_special_spatial.py`，保留基础读取器不变。
`AA8SpatialModeReader` 接收可选 `countdown_images={5:frame25204,7:frame28654}`
和 `buyin_overlay_image=frame29104`，不自动读取新的开发源。

- 购买倒计时只识别“购买剩余”固定前缀，排除后面秒数字；秒数保持未知。
  支持有实证的第3、5、7座，其余5个座位没有获得验证声明。
- 带入申请标题在有限画面区域内搜索，适应录制中浮层移动；正证据
  输出 `block_state_updates=True`，覆盖可能残留的保险匹配。
  没有匹配到该浮层不代表不存在其他遮挡。
- 已运行36张后期开发帧：25204第5座、28654第7座匹配购买前缀；
  29104/29254/29404/29554/29704/29855的带入申请都识别为阻断，
  标题相似度为1.0或约.979。其余30张没有该浮层误报。
  第二手4824的原第3座购买前缀仍匹配。
- 未加入保费数额、资金推断、模式触发或策略输出。
- 特殊模式相关22项单元测试通过，新文件flake8通过。

这些模板来自本次审核的同开发帧，是开发回归结果，不是独立验收。
