# AA8 v3旧开发回放的1283帧UNKNOWN分层

只处理`aa8_candidate_v2_development_v3/observations.jsonl`，不读图像、不跑识别。
新工具`tools/aa8_unknown_audit_v2.py`校验源清单development角色和逐帧源哈希。
输出`G:/PokerSense_private/aa8_unknown_audit_v2_dev_v2/report.json`包含全部连续段、
reason/街/actor分层。以下是旧v3诊断，不替代根任务随后v4/v5结果。

## 分类不是“普通桌准确率”

| 类别 | 帧数 |
|---|---:|
|有明确特殊模式暂停证据|107|
|正向发牌过渡|93|
|正常稳定帧等待规则触发|8|
|中心缺失或数值改变无法从旧记录区分|4|
|有当前行动者，但普通玩法未被确认|506|
|无当前行动者且玩法未确认|565|

合计1283。没有把“未检测到特殊模式”当成普通局；506只是优先核查的行动机会
候选，565也不能都算识别错误。用户已冻结特殊机制的具体规则/资金计算，本报告
只保留正向暂停，不把完成精算列为普通连续状态工作的前置条件。

## 最优先普通状态缺口

1. 2489–2810连续322帧：先5号ROI未知，2490的跟注58、余额减少58、底池增加58
   已确认；2503因12帧未知而丢弃基线，后续不能重建。根任务现保留私有账但公开
   UNKNOWN是正确的保守修复。不过2503–2810的308帧只有29帧全部ROI可判，最早
   2782、已近收筹；主要阻碍仍是5号58字体拒识。本子任务的新58银行已交付，
   具体全流收益待根任务v5回放验证，不能预测为322帧全部恢复。
2. 中心None被视为数值改变的缺口：旧数据触发帧1823、3185、4343、4501，同一
   UNKNOWN段后面分别还有224、195、58、27帧。但旧观测没有保存center当前值，
   无法区分None与真实数值改变，也不能把这些尾部帧全归因于一个bug。根任务已
   补日志并修复，旧报告只能给关联范围。
3. 初始/换手/收筹与无actor区间要分开评估，不能用缺失actor代替“无人行动”，
   也不能为了提高覆盖率把转场帧填成普通下注状态。

私有账保留必须遵守：只在没有已知数值矛盾且标题等于预期时保留；公开仍UNKNOWN。
一旦出现未定金额动作、真实数值矛盾、epoch/街变化、断帧或模式阻断，仍需清理。
重新发布前必须核对当前完整视觉分区，不能直接发布历史账来替代OCR失败。

工具5项测试通过；tests/tools/test_aa8_live_wagers_v2.py的单行缩进lint问题已修。
本报告不修改候选运行逻辑、冻结V1或用户刚刚冻结的特殊玩法规则计算。
