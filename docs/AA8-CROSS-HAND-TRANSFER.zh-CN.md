# AA8 冻结模板跨手开发验证

2026-09-09，结果PARTIAL。v5动作标记识别参数及模板保持不变，读取原录像
下一手所在窗口3320–5101，共1782帧（110.666–170.034秒抽取范围）。
窗口包含下一次发牌上下文，精确结束边界未登记，不称为完整手牌注册。

## 方法

新增`tools/aa8_action_transfer.py`，对照v5报告中的布局/模板来源及掩膜哈希，
固定0.90门槛、2帧确认、5帧未知后重新触发。源手与目标窗口不重叠；只读
development帧。识别器源码未改动，预测函数不读目标动作标签。预测后另行
人工核对，不是盲标金标准，也不是独立holdout。

逐帧输出保存在`G:/PokerSense_private/aa8_second_hand_transfer_v1`，
人工复核与预测比对在`aa8_second_hand_comparison_v1`。
目标帧在`aa8_second_hand_window_v1`；不要重复抽取或重新录制。

## 结果

17个已复核的可见动作标记匹配17个，标记漏检0，额外候选1：
4886帧3号座被识别为aggressive，4890完整画面明确为橙色“投保6”。
这是保险标签与加注徽标外观混淆，不是正常下注，必须作为负样本保留。
本轮没有用目标手去改模板或阈值；该失败结果原样保留。

不能说全部动作识别完成：4823帧0号余额104、底池474，4824帧余额0、
底池578并亮牌。连续4816–4831帧审阅显示倒计时直接变成亮牌/保险状态，
没有可用All in标记。这一资金变化与剩余筹码跟注全下相符，但属于需要
金额与行动上下文重建的动作，不应伪造一个视觉All in标签来计算漏检率。

6号上一手等待、这一手已参与并跟注后弃牌，不能沿用上一手等待状态。
Hero在本手翻前弃牌，后续牌局与保险画面仍有开发价值，不能丢弃。

## 测试及剩余工作

新增7项输入保护测试，覆盖连续帧、重复/倒序/缺帧、holdout、录制来源及
越界图片路径。Python lint通过；全量2743通过、1项跳过、2项依赖弃用警告。

下一步优先处理：

1. 区分“投保”与“加注”的文字证据和保险阶段，不能只靠橙色胶囊外形。
2. 结合自动金额读数重建未显示动作标签的跟注全下，同时保留UNKNOWN边界。
3. 登记本手精确边界、金额与结算，建立更多开发手的回归集。

这份结果不证明实时可用、策略可用、特殊模式规则正确或盈利能力。
没有访问采集设备、重启录制、进入实战或使用独立验收区间调参。
