# AA 简洁观察台与 DeepSeek 视觉复查

## 操作

- **观察**：默认页面只显示原画面、手牌/公共牌、底池、行动者、最近动作和标记按钮。八座筹码在折叠详情里。动作按最近优先显示。
- **标记此刻**：保存后台处理到的同一帧 JPEG、识别快照及规则版本，观察继续。标记不等于暂停视频，也不保证与浏览器当时的独立预览精确同步。点击“查看刚才标记”打开保存的固定画面。
- **复查记录**：最近30条；选中记录后填写核对范围和纠正内容。人工复查使用版本检查，防止跨页面覆盖；历史修订保留，原始 issue.json 不改写。标正确只代表操作者注明的核对范围。
- **DeepSeek 视觉复查**：在设置中输入 API Key；选中一条记录，勾选发送范围，点击发送。只发送这张冻结截图及其牌面、底池、行动者、各座筹码/投入/字样候选，可能包含截图上的昵称。不会读取当前/后续画面，也不会自动持续上传。
- **设置与分析**：来源、桌规、API设置集中在这里。原手工多人河牌分析与诊断信息在高级折叠区。回到观察页点击开始；设备只在明确选择采集来源并开始后打开。

## API 与数据合同

固定目标 `https://api.deepseek.com/chat/completions`，拒绝重定向。使用图片 base64 和 JSON 输出；默认 `deepseek-flash`，可选官方文档中的兼容名 `deepseek-v4-flash-vision-exp`。实际账户和别名映射以供应商为准。官方参考：[视觉输入](https://api-docs.deepseek.com/zh-cn/guides/vision)、[当前型号说明](https://api-docs.deepseek.com/quick_start/pricing)。

密钥只在服务进程内存，不进入磁盘配置、源码、返回状态或诊断消息；重启后重新填写。清除密钥阻止后续调用，已经发送的请求可能仍会结束。

默认每天20次，操作者可配置1–100次，按UTC日期计数并落盘；失败尝试也计数，重启不重置当日计数。每次最多4MiB图片、2200输出tokens，响应最多128KiB，无自动重试。**次数上限不是费用上限**，服务商实际收费另计。配置不发模型请求。

一次只允许一个网络worker，socket超时25秒，30秒停止等待并拒绝发布晚到结果。计时器不会强制杀死网络线程：底层线程仍收尾时保留busy，防止超时请求不断堆积。断线、模型错误或格式无效均不产生成功结果；不会回退到文字模型猜图，也不返回供应商原始错误正文。

每条模型结果绑定 issue ID、截图SHA、请求模型和prompt版本。AI返回仅包含摘要与match/mismatch/uncertain字段候选；结构不合法或输出截断会拒收。原始观测、人工说明、AI假设分别存储。界面用文本节点显示模型输出，结果轮询不覆盖未保存的人工作答。

人工与AI记录均不自动获得训练、独立验收或策略资格。本轮建立问题积累与复查入口，没有自动重训、自改代码或部署策略。误识别率需要有范围和分母的人工标注；不能从点击问题数量推导总体准确率。

## 验证

- 新增16项后台测试：冻结来源、历史修订、路径限制、图片哈希、密钥不落盘、明确发送范围、请求上限跨重启、超时晚到隔离、模型结构约束，以及真实路由流程。
- 26项确定性JS测试：包括三视图切换、标记后继续观察、跨记录迟到响应、API密钥输入清空、发送范围/预算限制、AI轮询不覆盖人工草稿。
- 全仓pytest通过（运行含1项依赖警告）；相关flake8与JS语法检查通过。
- 新EXE实际运行开发回放，源帧1261标记与JPEG哈希一致，停止后仍能重新读取并保存人工复查记录。这是软件流程检查，不是视觉准确率验收。
- 已使用用户授权的DeepSeek密钥完成一次真实视觉API请求，图片是程序生成的无个人信息测试图。模型识别出候选底池720与测试图120的差异，返回COMPLETE，551输入+252输出=803tokens；缺项标uncertain。测试结果不代表真实AA截图准确率。未上传真实牌桌画面。
- 浏览器连接仍失败，实际布局截图检查未完成；硬件/独立视觉/完整状态/高收益策略未验收。独立代码复审待完成。

私有交付 V4，默认本机8777；旧8775服务因工具拦截停止进程操作而保留。使用者应仅在新版开始采集，避免多个工具争用设备。旧目录保留用于回退。

## 已授权现场抽查轮次

用户明确表示正在现场使用并要求接通画面后，连接UGREEN 25854、设备0。此次DirectShow持续提供现场帧；MSMF在两秒内没有新帧而退出，不能沿用旧文档“只有MSMF可用”的设备结论。首张现场记录原帧141经查看，Hero黑桃K/梅花3与识别一致。仅证明该时刻连接和局部字段，不代表硬件长时验收或总体准确率；处理耗时也不是端到端延迟。

另经用户明确授权，将现场截图及对应字段发送给DeepSeek，启动一次有限抽查：`tools/run_aa_live_review_round.py`通过本机API操作，不读取密钥、不直接打开采集设备。必须显式传入`--confirm-live-image-upload`，最短间隔30秒、最多20次尝试。只跟随启动时的服务实例/generation；每次标记后再次验证保存的来源。停止观察、换来源、清除密钥或额度已满时结束；不确定的提交不重试。已有请求可能继续完成。

每次自动采样在私有round.jsonl里记录`EXPLICITLY_AUTHORIZED_TIMED_LIVE_SAMPLE`、issue/job ID、图片SHA和来源帧。这些是本轮授权的定时抽查，不能按单次人工标记或人工真值计数；原issue记录和AI候选继续保留。首张现场模型请求已返回COMPLETE，2599tokens；模型给出若干match和uncertain，仅属于AI复核候选。

本轮执行上限不会自动续期；回执保存于私有`aa-review-desk-20260915-v1/live-round-1`。4项新增调度测试验证授权、频率/总数、来源切换、繁忙与不确定请求；前端增至27项，修复其他页面/API切换来源后设置仍显示旧回放的问题。浏览器刷新不停止后台采集。
