# 旁观离线回放：底部筹码与观察上下文

本次仅对 session 001 的九手开发回放做定点修复，没有改变旧冻结布局、模型或
线上入口。新增布局为 `layout.observer-session001.candidate.json`，仍是候选。

## 底部筹码

原 slot 4 裁剪框 `[211,914,76,22]` 的全部13415次拒绝均为
`clipped_or_border`。原始样本全局帧2700人工可见1107，旧模型在裁剪框上移
1至4像素后均读出1107。新候选只将y改为912，其他座位和识别阈值保持一致。

对全部13415帧定点回放后，slot 4从0候选提升至13415候选。
这是覆盖改善；仅有一个人工样本核对，不是13415帧准确率100%的结论。

## 观察上下文

原发牌模板在本次全部帧中均未命中，自动新手检测没有触发。新增
ObserverDevelopmentContext仅供私有离线开发，要求调用者先核验源收据和边界
登记哈希，在登记首帧建立明确标注为MANUAL_DEVELOPMENT_BOUNDARY的上下文。
它不是自动发牌检测修复，不提供开局资金守恒、参与人数或合法状态证明。

每手单独实例化，错误源/区间拒绝，非首帧启动拒绝。遮挡、保险可见或帧缺口
会由原状态机清除上下文；不会凭人工登记在失效后自动重锚定。

有效epoch从0提升至12320/13415帧。H02的3931–4378及H11的17114–17760
仍为空，共1095帧：原日志在3931和17114将insurance标为VISIBLE，触发失效。
这是识别器的候选标签，需人工核实，不据此断言画面确实处于保险模式。
完整合法状态和策略资格保持0/false。

## 复验

7项聚焦测试通过；全仓3386 passed、7 skipped、1项依赖警告，完整lint通过，
299个策略夹具检查通过。定点回放仅重算底部筹码和观察上下文，未重新运行其他
字段识别；旧日志及由旧状态生成的下游结果没有被覆盖或冒充已更新。

私有证据：`G:/PokerSense_private/aa-development-001-fix-probe-v1/`。
输入日志SHA：`5ac0941bab6e9d8845a089fa0206d1df6c851af8ca75b9b7a6186159e236c167`。
定点输出SHA：`aa8e5ae7624d77bc006f4e45715295039315607d5ba3bde21f14f87977db614f`。
后续人工复核已完成：3931及17114都有购买倒计时，3940及17130清晰显示
保险模式。两处不是应消除的误检，继续保留上下文失效保护。

底部筹码7个开发抽查帧2700/3931/5000/7000/10000/16000/17114均与人工
读数一致。另提取的12000帧不属于原九手选择，只作画面上下文，不计入抽查分母。
抽查不是独立准确率验收，没有重新训练或放宽阈值。

此前“9手普通完整牌局”的分类不准确：H02、H11包含保险，应从普通整手选择
排除，保留为特殊场景保护回归素材。旧选择不变，新版development-selection-v2
保留7手、10068帧；已有定点日志在这7手中epoch有效10068帧。这个后验筛选不能
被用于声称对完整session的覆盖率100%，也不证明合法状态已闭合。

私有复核目录：`G:/PokerSense_private/aa-observer-visual-check-v1/`。
报告SHA：`1d2418ea3c63c3fe7e09765859d9c61733f22fd47df930a54d525547ed0aa907`。
新版选择SHA：`61a7613baca41730327ef88b1d7afbed4ad77ba64ccf2d54e302b97940c02133`。
下一步核对这7手中的动作、金额及状态恢复，保险场景保留在单独回归中。
