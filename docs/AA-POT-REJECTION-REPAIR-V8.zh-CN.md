# AA 底池拒识定位与修复 v8

状态 PARTIAL；没有达到四项整体验收门槛，策略仍关闭。

## 本轮修复

- 中心金币改用源图模板定位，避免仅按金黄色小色块判断时丢失图标。
  旧颜色路径仍保留为可复现的默认工具路径；组合读取器显式传入源模板。
- 在原中心字形基础上加入已人工核对的1380帧“14”两个字形。
  `center_bank_v2`共52字形，识别门槛保持0.90/0.05，未使用保留验收帧。
- Hero下注区域混入右侧灰色预操作按钮下缘，原来把60判为裁剪不完整。
  现按金币实际纵向范围截取数字，避免混入邻近UI。
- 标题“底池:480”的冒号后只有一列空隙，原先要求两列真实背景导致拒识。
  现验证确有空白分隔列后补背景边框，不把冒号带入数字；无分隔仍拒绝。

## 实测，不替代独立验收

| 指标 | 前一版v6b | 本轮v8 |
|---|---:|---:|
| 连续回放帧数 | 1801 | 1801 |
| 11检查点的底池核对正确 | 4 | 7 |
| 底池核对拒识 | 7 | 4 |
| 正面牌槽正确 / 负槽拒绝 | 36 / 36 | 36 / 36 |
| Hero余额正确 / 冷启动拒识 | 10 / 1 | 10 / 1 |

静态标题文字11/11正确；连续标题10正确/1冷启动拒识。
标题文字真值与完整底池账本不是同一概念，评分工具现在分别报告。

当前4处未输出核对值：

- 帧0：第一帧，尚无连续确认。
- 帧720：筹码移动遮挡中心数字，标题120与当前可见证据不足以闭合。
- 帧1200：标题480已经能读出，但中心240与结算动画中的下注显示尚未闭合。
- 帧1320：新一手已有14+2+4+8=28，标题仍0，保留差异而非强行选数。

v7轨迹中720之后729恢复120（容器时间约300ms），1320之后1340恢复28
（约667ms）。这是离线画面时间上的候选恢复，不是采集卡实测延迟。
1200结算段未核对出480，仍需动作/结算账本，不能靠延续旧值解决。
v8完整回放约85.76秒，测试同时运行，不能与先前耗时作严格性能比较。

## 验证和接续

新增灰色按钮边缘污染、冒号空白分隔的回归测试；全仓2656通过、1平台跳过、
2依赖警告，lint通过。原始标签、模型门槛、保留区间未改，失败版本保留。
私有根`G:/PokerSense_archive/aa_video_first_20260908`：`center_bank_v2`、
`combined_coin_geometry_v7`、`pot_separator_v4`、`combined_separator_v8`及score目录。
下一步应接动作和结算证据来解释未闭合资金，不再把所有拒识都归因于数字OCR。
