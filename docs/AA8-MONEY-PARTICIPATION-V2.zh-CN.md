# AA8 金额字库与等待文字 V2 开发候选

2026-09-09。仅新增V2文件，V1冻结读取器与原模型保留；
没有查看保留帧、保留真值、保留预测或300–600秒素材。

## 金额字库

工具 `tools/aa8_money_bank_v2.py` 合并：

- 首手结算夹具中全部货币检查点、开局及结算前后余额标签；
- `eight_dev_checkpoint_gold_v1.json` 的12个已人工审核开发检查点；
- 直接打开原始PNG重新确认的5050和5051全部八座余额，不只挑某个数字。

5050座位0–7：0、88、502、574、510、204、194、246。
5051座位0–7：0、280、499、570、504、202、192、244。
源码记录两个源PNG哈希；每个训练字形记录帧、字段、数字位置、
源哈希和特征哈希。重复帧/字段去重，矛盾标签或源哈希拒绝。

未改字体识别阈值：floor=.90、margin=.05；沿用增广方式。
不做底池/余额算术纠错，不从缺失座位制造0。

最终当前产物：
`G:/PokerSense_private/aa8_reviewed_money_bank_v2_boundary/bank.npz`。
旁边 `report.json` 包含所有训练与排除记录。
较早 `aa8_reviewed_money_bank_v2`、`..._repeat` 均保留。

378个训练字形；同开发标签回读137字段中133匹配、4未知、0错误。
这是训练内开发检查，不是独立识别准确率。

明确保留的排除/未知：

- 1650底池81、2550底池231、4050底池76：现有前缀提取返回空；
- 3195 Hero516：当前余额图块出现边缘裁切证据；
- 1262七个起始余额：帧不在传入的首手完整池中，逐项记录缺源而非静默跳过；
- 首手九个检查点的第6座无可用数字，不当作0训练。

## 等待文字空间对齐

新增 `tools/aa8_participation_v2.py::ParticipationReaderV2`，继承原接口。
仅使用首手2400第6座已有“等待下一手”黄色文字模板，
按文字包围盒归一化到固定大小，修正小幅空间平移和边距差异。
仍用.90阈值；过少笔画、过密黄色区域或不合理文字尺寸不接受。
保留其他已有正证据的冲突检测，不覆盖扑克参与历史。

已读首手1320/1500第6座为空，2400/2550/2700/3150等待下一手，
3319等待补盲/过庄；这些原开发结果保持。
合成平移测试验证对齐，不宣称已通过其他座位独立真值验证。

14项本轮单元测试通过，新增代码flake8通过。
这两项是下一版本候选，不能改写V1的独立测试结论或宣称视觉全部验收通过。

## 中央数字字体扩展

新增 `tools/aa8_money_center_bank_v2.py`，基于上一份八座边界字库追加
预先列出的11张开发帧中央硬币前缀数字。每张都先打开完整PNG直接阅读，
不根据底池标题或模型猜测标签：

- 1320/1500/1980：14；
- 2070/2400/2700：115；
- 2850/3150：289；
- 3390：16；4402：114；4755：190。

通过不返回预测的 `CaptureBank` 捕获已有 `AACenterAmountCandidate` 送入
`diagnose` 的真实图块，完全复用原有裁切流程，不另造中央裁切几何。
全字库按特征哈希去重，特征相同而数字不同立即拒绝；原字库报告链接
与所有旧排除项保留，新增每个中央字段和字形记录源哈希。

产物 `G:/PokerSense_private/aa8_reviewed_money_bank_v2_center/bank.npz`：
旧378字形去重后126个，加16个中央字形，合计142个独特特征。
参数仍为.90/.05并增广，不做金额算术纠错。

中央11个开发字段全部匹配；连同原137字段回读共148字段：
144匹配、4未知、0错误。未知仍为之前的3个底池前缀和1个余额裁切，
不能通过字库扩展消除几何缺口。新增2项测试通过，新增代码flake8通过。
这仍是开发训练内回读，不是独立验收准确率。
