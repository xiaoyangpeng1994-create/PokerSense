# AA 四项关键工作：字段与连续链第一轮

状态 PARTIAL；四项均未全部验收。策略和实战没有启用。

## 1. 逐座位字段

已接九槽灰度金额识别候选，使用 AA 自身原片字形，不改 WPK 的生产模板。
从帧0的8个明确余额分割字形；初始裁剪包含胶囊边缘，已通过固定横向内缩解决，
不是按预测数值动态改裁剪。后续增加原先已冻结真值中的1200/1320/1740 Hero 字形。

同一已曝光29700检查点，首版九槽6正确/3拒识；补充字形后8正确/1拒识，
没有测得错误接受。1054仍拒识，不能把内部 raw_text 当作已接受的值。
门槛仍0.90/0.05，没有降低；0只代表屏幕数字，不等于仍在局或可用筹码。

新增弃牌/All in 可见文字候选。29700模板所在帧的6处目标文字中接受4处、拒识2处；
3处非目标不误认；0帧9个非目标均拒识。17100被现有场景门禁拒绝，
所以6处可见弃牌全部暂缺，不能算识别正确。动作正确率未做独立验收。
当前跟注、加注、下注、看牌含义、当前行动者及完整占座普查仍未打通。

## 2. 特殊模式

完成公开规则来源核对和字段分层，见 `AA-SPECIAL-MODES-EVIDENCE.zh-CN.md`。
未获得足够 AA 专属规则证据，不套用其他平台。暴击/鱿鱼/保险的 enabled 与
triggered 均分别输出未知；这只是安全接口，不是已完成识别器。

## 3. 移动牌与连续处理

- 直接以色彩中性背景定位唯一牌面上边界，未按预测点数/花色选择位置。
- 首次全部动态搜索退化为27正确/9拒识，失败保留。修正为固定位置有效时保留固定裁剪，
  只有牌面明显延伸到裁剪上方才搜索新上边界。
- 同一11检查点的静态结果：36正确、36负样本拒绝、0错误/拒识。
  这是开发数据上的修复，不能宣称 AA 牌面全通过。
- 新离线组合读取器顺序处理0–1800，共1801真实连续帧，核对原始时间及像素锚点。
  牌面用真实帧序号的融合生命周期；金额/文字需相邻两帧一致；中断、场景失效、
  未知字段立即清当前展示。独立移动的公共牌改变 ROI 时单槽融合重置。
- 本机首轮58.59秒处理1801帧；这是离线含解码吞吐，不是采集卡端到端时延。
- 复跑约52.14秒，observations.jsonl与首轮字节完全一致，SHA256为
  `7f86f4cd631dfae997b2fe7dc8ce42fda819f6b52d8b0127f56e6173e18161bc`。
  复跑不是新增独立样本。全仓2626项测试通过、1平台跳过、2依赖警告，lint/diff通过。
- 连续结果在11真值点得到牌槽27正确、9拒识、36负样本拒绝；Hero金额9正确/2拒识。
  静态好于连续的原因须继续审计，不能用静态成绩替代连续链。
- Hero展示在152出现Jc9h，1261清除，1433出现Ac9c；期间存在若干短暂拒识。
  这是自动展示变化，不是已经人工确认的完整手边界或动作账本。
- pot、seat_presence、current_actor、完整动作和特殊模式仍显式缺失；
  所有输出 `strategy_eligible=false`。

## 4. 独立验收和长时运行

在调参前冻结三个尚未用于模型调参的候选区间：2701–3599、6301–7199、18001–18899。
训练入口会拒绝这些区间。只有整手完全落在一个区间内且边界核实，才可登记为验收手。
当前已确认的独立手数仍为0；区间可能不含完整手，也不是独立设备/会话验证。
不能先跑全片调参，再声称里面的片段是独立验收。全片连续识别、独立模式样本和实机
断连/时延验收仍待完成。已完成的全片解码建档不重复。

## 私有证据

根目录 `G:/PokerSense_archive/aa_video_first_20260908/`：
`amount_candidate_v1`、`amount_candidate_v2`、`action_candidate_v1`、
`cards_dynamic_top_v1`（失败）、`cards_dynamic_nominal_first_v2`、
`combined_0000_1800_v1`、`combined_0000_1800_score_v1`、
`combined_0000_1800_repeat_v1`。各目录保留哈希清单；代码测试结果在 AGENTS.md。

下一步：补完金额与动作覆盖、真实占座/参与普查和底池；审计连续拒识与精确换手；
定位特殊模式样本；在冻结模型后审核保留区间中的完整手。不得标记 AA 已可搭策略层。
