# WPK 视觉当前工作队列

更新：2026-09-08。顺序由用户最新指令固定：视觉 → 策略 → 另行确认实战。
当前状态：IN_PROGRESS。完成标准见WPK-VISION-ACCEPTANCE.zh-CN.md。

用户最新修正：不需要定时任务，要求当前对话立即连续推进。自动任务pokersense已暂停，
不得自行重新开启；以下旧调度记录仅保留历史。

## 立即推进

最新具体目标：`s002_001197_td3h`，1197–1595共399帧。先补齐动作/状态真值，再自动重建
对照；不要运行上下文1196/1596作为本手计分，也不要将开发目标重新称为独立验收。
首版三手边界/模型快照已冻结，独立验收手数仍0，详见WPK-HAND-REGISTRY-V1.zh-CN.md。

最新结果：本手14个常规行动已由当前像素生成的证据时间线对齐，金额/顺序匹配且无多造行动。
仍未成为完整合法下注历史；当前转向强制投入、离座、收注/退回等规则对齐及跨手覆盖。
不要把观察到的净余额变化当作已确认费用，也不把新来占座者自动加入本手。

最新进展：已重建经目检的session001灰度筹码/底池库，等待200与多数低分数字已改善，
并完成实验性同帧组合及原片回放。整套仍为PARTIAL；当前重点转为独立标定、参与资格、
连续事件/状态和界面/长运行验收。下列条目保留完整目标，不把组件进展当作已全部闭合。

1. **灰度数字模板与金额覆盖**：查清已有28×28模板的原始来源/缩放/背景；用既有录像
   重新整理独立来源字形，区分亮/暗和位置变化。保留v1裁剪方案作基线，不采用已失败的
   二值原型。重点包含等待200、容易混淆数字、长金额及小数；不能用牌局数学填OCR空缺。
2. **对手全押与动作完整性**：不同座位/字体/特效的独立样本；保留动作v2、0.85/0.10
   工程门槛的原始结果。候选通过后还需独立校准；不能将持续文字逐帧重复入账。
3. **参与状态与视觉资格**：复用上下余额位置选择，但分开实际入座、等待审核、在局、
   弃牌、旁观、轮到本人；缺依据留UNKNOWN，已有录像优先。

## 随后推进

4. 支持矩阵与按真实手牌隔离的锁定数据集；冻结阈值与统计口径。
5. 将已独立验证的视觉组件接入正常识别路径，输出到软件界面；候选不能一直只留在工具里。
6. 自动视觉→动作/状态链：同帧不同步、漏多步动作、主/边池、前注/盲注/straddle的视觉
   输入及合法性校验；未知金额/动作阻止确认，不凭最终派奖倒推动作。
7. 完整真实录像回归、长运行、错误恢复、误接受/拒答与有效状态覆盖报告。
8. 逐项审核V-A至V-H；全部达标才出VISION_OFFLINE_READY并通知。真实设备阶段另外确认。

## 已有基线，避免重做

- 卡牌候选v9与独立环境已建立；生产requires_revalidation=true。
- 动作v2：开发34/35、旧后续7/7、新动作检查10/11正确；仍有对手全押拒答。
- 筹码最佳当前候选是v1裁剪方案：开发33/63；后来的二值原型不采用。
- 本人余额位置：13点全对；82密集帧65位置正确/17拒答，无选错。数字提升主要为审核
  状态显示0，不能记成可用筹码覆盖。等待200数字问题未解。
- 已审阅输入的完整手牌重放30节点通过，但不等于自动视觉事件链完成。
- 最近全量回归2526通过、1平台跳过、2依赖警告；各证据详情见AGENTS当前状态和专项报告。

以上为本轮前基线。最新：灰度筹码62/63、等待布局13/13数字；组合486帧检查点底池5/5、
底牌/公共牌28个正槽位全对，六人占座另61帧。最新回归2549通过/1跳过。
详见WPK-GRAY-BANK-COMBINED-VISION.zh-CN.md，原先固定裁剪和二值失败记录不删除。

## 交接记录要求

每轮在此文件末尾追加简短记录：实际完成项、证据路径、PASS/PARTIAL/FAIL、下一项。
重复的安全阻塞只记录状态变化；需要用户时一次性说明最小所需资料，不伪造完成。

- 2026-09-08：按用户新指令收敛为视觉唯一主线，建立完整验收标准和顺序队列。
  本轮未增加识别准确率，未启动策略或实战；准备在当前任务中持续推进。
- 2026-09-08：已创建并核实当前任务的持续推进心跳“PokerSense 视觉持续完善”，
  自动任务id为pokersense，状态ACTIVE，每30分钟唤醒；当前任务id
  01a07d02-6cbb-73d1-a52b-2963457cf19d。需要本机及应用运行、仓库和G盘可访问。
  普通推进记录在此，完成/重大问题/需要用户时通知；完成或真正需要用户时暂停自动任务。
  文档差异检查通过；本轮仅改变优先级、验收和调度，未宣称新增视觉性能。
- 2026-09-08：按用户明确修正，pokersense已暂停，改为本对话内直接实施。
  重建灰度库、剔除9个错误字形、底池补齐十位数字、正常识别器接口、组合原片回放、
  六人占座检查和场景拒绝已完成开发验证；总体PARTIAL。不得重新设置定时器或宣称
  后台会自行继续。剩余为独立标定/参与状态/自动事件链/UI/长运行，策略与实战未启动。
- 2026-09-08：完成三手真实边界登记及当前模型/输入冻结；新增六人Td3h完整401帧来源窗口，
  399帧归属、2帧上下文。查出旧分组跨手和训练接触，未把冻结回归冒充独立验收。
  2564测试通过/1跳过，未评分识别器。下一项：Td3h逐动作与状态真值、自动时间线对照。
- 2026-09-08：Td3h普通行动真值冻结后，首轮11/14；加入可选徽标尺寸容差及第一段录像
  让牌变体后14/14，顺序/金额匹配、多余行动0。未把离座6编造成弃牌，未添加等待座位5。
  旧三组动作回归无新增错误/丢失正确接受，2581测试通过/1跳过。仍是候选观察证据，
  不是正式StateEvent；下一项为规则对齐和跨牌局回归，详见WPK-TD3H-VISUAL-TIMELINE.zh-CN.md。
