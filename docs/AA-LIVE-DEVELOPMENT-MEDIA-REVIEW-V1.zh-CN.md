# AA 旁观开发采集 001 媒体审查

## 结论

`aa-live-observer-development-20260914-001` 的技术、平台连续性和私有画面审查
通过；隐私审查要求排除开头 75.600 秒。审查得到 9 手普通、无遮挡、两端均有
可见转换的完整时序牌局，并按“新一手首次可见多座强制下注变化”绑定到唯一源帧
和真实容器 PTS。这是该私有源和该边界定义内的 canonical 登记，不是跨 session
验证集、校准数据或实战策略证据。

源最终收据 SHA-256 为
`c09dd51e737cfb57bd001f716e6d75e8c29d4fda9c840b70d04109c9bc96748d`。
审查开始前重新核对 10 个分段，共 3,401,350,388 字节，10/10 文件的大小和
SHA-256 均与收据一致。本次只读取该 session，没有读取其他录像或上传媒体。

## 技术和平台连续性

- 10 个分段均可全量解码，错误日志均为空。
- 全部为 1920×1080、30 FPS MJPEG 单视频流，无音频。
- 录制开始 0.000–0.867 秒为黑屏；之后没有新的黑屏事件。
- 每秒抽取 600 个样本，600 个逐帧哈希全局唯一。
- 十张逐分钟联系表持续显示同一个 AA POKER 八座牌桌，没有切到大厅、其他 App
  或采集卡启动画面。
- 核心画面持续符合既有 `x=[711,1209), y=[0,1080)` 的 498×1080 几何。
- 样本没有显示 Hero 洞牌或供操作者点击的弃牌、跟注、加注控制，与操作者声明的
  旁观状态一致；视频不能独立证明操作系统层面完全没有输入事件。

## 隐私阻塞区

录制开始后出现持续到 75.600 秒的来电横幅，包含直接电话号码。号码值没有抄入
任何 JSON、文档或 Git 文件。`0.000–75.600` 必须作为一个整体从所有派生数据中
排除，不能靠裁掉顶部后继续使用，因为横幅也遮挡了桌面顶部区域。

其余画面仍包含昵称、头像、筹码/下注、可能的房间或桌号、手机状态栏和实时牌局
状态。原始录像、联系表和边界图只能保存在 `G:/PokerSense_private`；不得提交
Git 或公开上传。任何对外共享前都要遮挡并使用玩家假名。

## 完整牌局候选

只读探针在 5 FPS 上读取公共牌数量和庄家标记，不识别牌面、昵称、金额或策略。
随后对 12 个转换候选各取前后 2 秒、5 FPS 的密集联系表进行人工复核。

审查结果：

- 12 个已复核的转换窗口；
- 录制头部缺少前序开局，录制尾部在新一手中结束，各保留一个不完整区间；
- 相邻转换之间有 11 手时序完整候选；
- 其中 1 手受到直接电话号码横幅遮挡，排除；
- 其中 1 手包含“幸运暴击”特殊结算，保留但从普通基础数据排除；
- 剩余 9 手是普通、无遮挡、两端均有可见转换的完整牌局候选；
- 9 手中包含 2 手疑似翻牌前结束的短手，仍属于完整时序候选。

第一轮转换窗口宽度约 1.4–2.6 秒。随后在每个窗口内读取 30 FPS 的公共牌、庄家、
强制下注像素和发牌动画信号，并对候选首帧的前 3、前 1、当前、后 1、后 3 帧进行
人工复核。12/12 候选都满足：候选前一帧尚未出现新一手强制下注变化，候选帧开始
出现多座同步筹码扣减或强制下注标记。B09 在特殊结算动画结束后单独绑定。

FFprobe 为全部 18,003 帧建立了独立 PTS 索引。精确边界为：

| 边界 | 全局帧 | PTS 秒 |
|---|---:|---:|
| B01 | 1322 | 44.067 |
| B02 | 2591 | 86.367 |
| B03 | 4379 | 145.960 |
| B04 | 5894 | 196.453 |
| B05 | 7342 | 244.712 |
| B06 | 9270 | 308.969 |
| B07 | 10654 | 355.095 |
| B08 | 11884 | 396.089 |
| B09 | 13639 | 454.580 |
| B10 | 15441 | 514.638 |
| B11 | 16202 | 540.001 |
| B12 | 17761 | 591.959 |

边界之间的 9 手普通、无遮挡牌局已经冻结为 `development_only`，共 13,415 帧。
H01 因直接标识遮挡排除，H08 因特殊结算从普通基础数据排除；头部和尾部仍不完整。
这个选择没有 validation、holdout 或 calibration 角色，不能用同一 session 内拆分
冒充独立验证。

## 证据与下一步

私有证据目录：
`G:/PokerSense_private/aa-live-observer-development-20260914-001-media-review-v1/`。

- 最终报告：
  `7a30153d487a2d4d28db1c284efe1cc8d0afb1015a403df762f645b086a17124`
- 初步窗口复核（由精确登记取代）：
  `76f1472af39992fa94040cba42d8a37f384bf447b024285bac997ad1cc2e4aa2`
- 精确边界登记：
  `63a605ccd5e750d95e02cc6c1d8ec946a654d61811edfa18fe417933c7acb0b2`
- 18,003 帧 PTS 索引：
  `26d3a815f5b45e59d6abf2030dbcd7c405bbbce69386e879069813180957d9fb`
- 9 手牌局开发选择：
  `08a8f411d2df4c2af73402adca47a2a02efadcf8794492a98ec7566d5c62f4cc`
- 隐私复核：
  `780e53c9f62d7d1993fa9417966fd72583942cca3af8217be8c705a219bc02d4`
- 79 文件最终清单：
  `6ff0efe4f0028c7679229f09e0c93b81108fc138af3988163747f26fe08756c4`

下一步是在这 9 手冻结的 `development_only` 选择上运行现有离线识别回放。该操作
属于新的处理范围；在获得明确授权前，不运行识别模型或生成状态/动作结果。真实
规则、稳定玩家身份、完整决策机会清单、合法动作菜单和第二个同规则独立 session
仍缺失，所以 `ready_for_calibration=false`、`strategy_eligible=false`、
`advice_emitted=false`。
