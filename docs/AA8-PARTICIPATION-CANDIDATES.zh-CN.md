# AA8 参与者正证据候选

2026-09-09；仍为 DEVELOPMENT_CANDIDATES_NOT_ACCEPTANCE。

新增 `tools/aa8_participation.py`。只读现有开发 PNG；1320 帧提供可见背牌与
6 号空座加号，3319 帧提供 6 号“补盲或过庄”文本模板。左右各座背牌按本座
模板比较，6 号尚无本座首手背牌，暂借同侧 5 号模板，保持候选而非真值。
颜色加白色边缘两项均达阈值才输出对手背牌；Hero 大背牌采用独立矩形，
不会把对手小牌坐标硬套过去。

首轮全帧后发现跨座背牌样式对齐差异，保留 v1，增加每座模板后的 v2：

| 开发窗口 | 帧数 | EMPTY 座帧 | BACK_CARDS 座帧 | WAITING 座帧 | UNKNOWN 座帧 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 首手 | 2057 | 1031 | 5505 | 149 | 9771 |
| 次手及尾部窗口 | 1782 | 0 | 3271 | 0 | 10985 |

这些是候选输出计数，不是准确率；每帧 8 个座位，所以座帧不等于手数或人数。
1320 七座背牌加 6 号空位、3319 的 6 号等待文本与人工看图一致，但这两帧
本身就是模板来源，不能作为独立测试。次手已是开发回归，也非 holdout。

`RosterCandidates` 连续确认显式 cues，并可接收已有自动 fold glyph；缺少
按钮、背牌或文字一律 UNKNOWN，不推断旁观/弃牌。已确认入手者不会因为
后续等待文本被悄悄从历史名单移除；该冲突上报 UNKNOWN。新手身份须由外部
可靠边界提供，当前工具没有假装自己完成自动换手或完整参局人数识别。

根任务本轮精确登记次手为 3320–5050、下一手首帖 5051。5036 底池已清零，
所以底池归零不能直接当下一手开始。5051 1 号 88→280 同时有加码，不能算
赢利。该帧 0 号“留座179秒”是新等待类型，本工具尚未识别，返回 UNKNOWN，
不能因为头像和数字存在就加入下一手。这里仅记录人工边界结论，不把边界
fixture 输入检测器或使用根任务参考标签帮助预测。

私有输出：`G:/PokerSense_private/aa8_participation_first_v1/v2` 和
`G:/PokerSense_private/aa8_participation_second_v1/v2`（各自独立目录）。
14 项时序单元测试通过；验收/冻结/参与者全部 56 项通过，代码 lint 通过。
不启动设备、录制、策略，也未读取 holdout。

## v3：等待下一手文字

根任务12个开发检查点比对发现首手6号黄色“等待下一手”没有被旧绿色
“补盲或过庄”模板覆盖。新增可选 `waiting_next=None` 构造参数；传入已人工
审阅的开发2400帧后，裁切6号余额区域提取黄色文字形状，与每座当前区域
做0.90同阈值匹配，输出WAITING_NEXT_HAND。不是因为没有数字就认定等待。

首手2057帧重跑保存到 `G:/PokerSense_private/aa8_participation_first_v3`。
新增848个WAITING_NEXT_HAND座帧，UNKNOWN降为8923；原EMPTY1031、BACK5505、
WAITING_POST_OR_PASS149保持不变。2400/2550/2700/2850/3000/3150的6号均明确
匹配黄色等待文字，score=1.0。2400是模板来源，此结果仍属开发，不是独立准确率。

空座→等待不会误报已入手冲突；当前UNKNOWN不能从过去等待历史补成NA。
下游只能在当前正证据存在时把不存在的余额标成NOT_APPLICABLE，不应直接把
history=WAITING用于金额覆盖。17项参与者聚焦测试及代码lint通过。
没有更改人工真值或旧输出，源2400图片SHA已写入v3报告。
