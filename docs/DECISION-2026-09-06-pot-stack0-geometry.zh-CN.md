# 已结案：采集卡 pot / stack[0] / stack[4] 几何语义与标签审计（2026-09-06）

> Phase 2（采集卡九字段全通）。owner 授权「Q1/Q2/Q3 按最优方法解决」，
> 全部结论经 6x/8x 放大像素目检锚定，双字段标定块已落 calibration.json，
> 回归 2283 passed / 1 skipped，flake8 净。

---

## 一、最终语义地图（证据链锁定，推翻旧稿）

| 字段 | 位置 | ROI (x,y,w,h) |
|---|---|---|
| **pot** | **顶部居中墨绿彩带** | (0.400, 0.256, 0.200, 0.030) |
| **stack[0]**（hero 筹码） | **底部黑条胶囊** | (0.4016, 0.9704, 0.2410, **0.0194**) |
| **stack[4]** | **顶中头像下黑色胶囊** | (0.415, 0.194, 0.170, 0.028) |

旧稿曾判「底部胶囊=pot」，依据是 pot 标签同手单调递增——但标签本身双源混杂
（循环论证）。反转证据链：

1. **下注按钮数学**：session_001 frame2 底池下注按钮 28/42/56/84/100
   = 84×{⅓,½,⅔,1,1.2} → 真 pot=84 = 顶部彩带读数 ⇒ 底部胶囊 162≠pot。
2. **空座位彩带**：空座位仍显示 84（空位不可能下注，彩带不是下注额）。
3. **胶囊跨手递减** 222→162→144：输牌轨迹，是 hero 筹码不是 pot。
4. **让牌玩家彩带 30≠其下注**：彩带不是个人下注。
5. slot4 生产 ROI 原钉在彩带上（错），真身是头像下黑胶囊（192/168/64/790…）。

## 二、执行记录（owner 授权后）

### 1. 标签像素审计 ×3 轮（全部留痕 + 备份 + 前缀碰撞守卫）

- **R1**（126 处）：pot 87 修正 + 4 例 CONFLICT（`总底池` 结算屏）；
  stack 35 修正（slot0×26、slot1×2、slot4×8、slot7×1）。
  备份 `frames.jsonl.bak_pixel_audit_20260906`。
- **R2**（24 处）：以重建后的识别器作探测器（eval/train 高分分歧全量目检），
  **24/24 全是标签错、识别器对**。错误家族：首位 4 误标 3（424/418/414/412/410/
  415/341×2/242/289）、末位 4/5 误标 3/6（374/174/225/195）、截断丢位
  （240/240/228/271/872/358×3）、中位 4 误标 3（249）。
  备份 `frames.jsonl.bak_pixel_audit_r2_20260906`。
- **R3**（9 处）：slot0 occupancy **EMPTY→OCCUPIED** + 按像素补 stack 值
  （144/104/142/140/140/1396/306/64/300）。auto-empty 标注器在 hero 座位
  系统性误标（底部无头像圈），9/9 帧 hero 均持牌在座。
  备份 `frames.jsonl.bak_pixel_audit_r3_20260906`。

### 2. split 微调（45f191 移入 train）

pot 含数字 7 的帧仅 4 个且原全在 eval。45f191（pot=671）为孤立标记帧
（无同手兄弟标记，手牌隔离不受损；eval 仍留 793×3 作诚实 7 测试）。
train 229 / eval 138。备份 `splits/*.bak_20260906_7move`。

### 3. 生产缺陷修复：拓扑门网格对称化（`_match_char`）

彩带「184」曾被读成「134」@0.861（false VALID）。根因：细体 8 在 15×10
原始尺寸 Otsu 后孔洞塌陷（0 孔），拓扑门把 2 孔的真 8 模板**踢出候选**，
0 孔组里 3 获胜——而 8 模板裸分高达 0.982。修复：输入侧与模板侧孔数
**统一在 28×28 归一化网格上统计**（letterbox 放大保住孔洞；胶囊粗体字体
两种口径一致，生产模板幂等）。+1 回归测试
`test_amount_match_counts_holes_on_normalized_grid`。

### 4. 模板与标定块（hand-isolated，train 建模板 / eval 测量）

| 字段 | 模板集 | eval 正样本 | correct | false VALID | floor | ceiling | 可分 |
|---|---|---|---|---|---|---|---|
| pot | `digit/`（0-9 齐，孔数规范） | 35 | 20 | **0** | 0.8936 | 0.7793 | ✅ |
| stack | `stack_digit/`（新 ROI 重建） | 130 | 34 | **0** | 0.8001 | 0.7988 | ✅ |

负样本 81（EMPTY 座位，0 泄漏）。召回偏低（57% / 26%）= 主动 UNKNOWN，
符合失败即关闭。min_score=0.8 恰落在两字段 floor/ceiling 间隙内。

## 三、方法论沉淀（后续字段复用）

- **识别器即探测器**：新模板上线后，把 eval+train 高分分歧帧全量目检——
  本轮回合借此捞出 33 处标签错（24 值错 + 9 occupancy 错），识别器 0 错。
- **标签错误家族**：4↔3 互混、6↔8、截断丢位、auto-empty 在 hero 位误标。
- **孔洞拓扑必须同网格对比**：输入与模板任一侧在原始小尺寸统计都会被
  Otsu 塌陷反噬。
- 彩带偶现 `总底池` 结算屏 → CONFLICT；pot 无自动负样本，ceiling 来自非
  correct 观测。

## 四、数据集位置

`G:\PokerSense_archive\`（2026-09-06 迁自 C 盘，robocopy 校验，零丢失）：
`capture_card_calibration_20260903\`（wepoker）、`capture_card_calibration_aa_20260904\`（AA）、`_delivery\`。
工具链 `--root` 一律指 G:。标签备份与修正脚本（`_debug_apply_label_corrections*.py`，
gitignored）留工作区备查。
